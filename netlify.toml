[build]
  publish = "_site"
  command = "curl -fsSL https://quarto.org/download/latest/quarto-linux-amd64.deb -o quarto.deb && sudo dpkg -i quarto.deb && quarto render"

[build.environment]
  DEBIAN_FRONTEND = "noninteractive"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
